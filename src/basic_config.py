# ALL custom configs must have these fields
config = {
    "action_frequency": 5,
    "initial_state": "../states/post_start_screen.state",
    "rom_path": "../ROMs/Metroid2.gb",
    "seed": None,
    "max_steps": 5000
}